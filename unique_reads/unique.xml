<tool id="unique_reads" name="Seperating unique reads" version="0.1.0">
    <requirements>
    </requirements>

    <stdio>
        <exit_code range="1:" />
    </stdio>

    <command><![CDATA[
      mkdir output_dir
      &&
      python $__tool_directory__/unique_reads.py
        -i $infile
        -o output_dir
    ]]></command>

    <inputs>
        <param type="data" name="infile" format="bed" />
    </inputs>

    <outputs>
        <!-- <data name="bed_file" type="bed">
            <discover_datasets pattern="(?P&lt;designation&gt;.+)\.bed" format="bed" directory="output_dir" />
        </data> -->
        <collection name="bed_file" type="list" label="unique_reads.bed">
            <discover_datasets pattern="(?P&lt;designation&gt;.+)\.bed" format="bed" directory="output_dir" />
        </collection>
    </outputs>

    <tests>
        <test>
            <param name="infile" value="testbed.bed"/>
            <output name="unique_reads" file="out/unique_reads_0.bed"/>

            <output name="unique_reads" file="out/unique_reads_1.bed"/>

            <output name="unique_reads" file="out/unique_reads_2.bed"/>

            <output name="unique_reads" file="out/unique_reads_3.bed"/>

            <output name="unique_reads" file="out/unique_reads_4.bed"/>
        </test>
    </tests>

    <help><![CDATA[
**Unique Reads**
    ]]></help>
</tool>
